# Copy to config.py to customize (config.py is gitignored).

owner_name = "Ardha"

# LM Studio local OpenAI-compatible API (Developer → Local Server).
LM_STUDIO_BASE_URL = "http://127.0.0.1:1234/v1"
LM_STUDIO_API_KEY = "lm-studio"
# Must match the model id LM Studio shows when loaded (Server UI / API logs).
LM_STUDIO_MODEL = "local-model"
